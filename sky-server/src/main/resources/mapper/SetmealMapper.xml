<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetMealMapper">
    <select id="pageQuery" resultType="com.sky.vo.SetmealVO">
        select s.*,c.name as categoryName
        from setmeal as s
        left join category as c on s.category_id=c.id
        <where>
            <if test="name!=null and name!=''">
                and s.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId!=null and categoryId!=''">
                and s.category_id=#{categoryId}
            </if>
            <if test="status!=null and status!=''">
                and s.status=#{status}
            </if>
        </where>
        order by s.update_time desc
    </select>
    <insert id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        insert into setmeal(name, category_id, price, status, description, image, create_time, update_time, create_user,
                            update_user)
        values (#{name}, #{categoryId}, #{price}, #{status}, #{description}, #{image}, #{createTime}, #{updateTime},
                #{createUser}, #{updateUser})
    </insert>
    <select id="getByIdWithCategoryName" resultType="com.sky.vo.SetmealVO">
        select s.*, c.name as categoryName
        from setmeal as s
                 left join category as c on s.category_id = c.id
        where s.id = #{id}
    </select>
</mapper>